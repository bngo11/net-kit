--- a/lib/nfs_v3.c	2026-07-15 19:53:22.305611199 -0500
+++ b/lib/nfs_v3.c	2026-07-15 20:21:45.003241145 -0500
@@ -3205,7 +3205,7 @@
 nfs3_mknod_async(struct nfs_context *nfs, const char *path, int mode, int dev,
                  nfs_cb cb, void *private_data)
 {
-	char *ptr;
+	const char *ptr;
 	struct mknod_cb_data *cb_data;
 
 	cb_data = malloc(sizeof(struct mknod_cb_data));
@@ -3223,8 +3223,8 @@
                                       "buffer for mknod path");
                         return -1;
                 }
-                ptr = strrchr(cb_data->path, '/');
-                *ptr = 0;
+                char *ptr2 = strrchr(cb_data->path, '/');
+                *ptr2 = 0;
         } else {
                 cb_data->path = malloc(strlen(path) + 2);
                 if (cb_data->path == NULL) {
@@ -3314,7 +3314,7 @@
                   void *private_data)
 {
 	char *new_path;
-	char *ptr;
+	const char *ptr;
 
         ptr = strrchr(path, '/');
         if (ptr) {
@@ -3324,8 +3324,8 @@
                                       "buffer for unlink path");
                         return -1;
                 }
-                ptr = strrchr(new_path, '/');
-                *ptr = 0;
+                char *ptr2 = strrchr(new_path, '/');
+                *ptr2 = 0;
         } else {
                 new_path = malloc(strlen(path) + 2);
                 if (new_path == NULL) {
@@ -3573,7 +3573,7 @@
                   int mode, nfs_cb cb, void *private_data)
 {
 	struct create_cb_data *cb_data;
-	char *ptr;
+	const char *ptr;
 
 	cb_data = malloc(sizeof(struct create_cb_data));
 	if (cb_data == NULL) {
@@ -3590,8 +3590,8 @@
                                       "buffer for creat path");
                         return -1;
                 }
-                ptr = strrchr(cb_data->path, '/');
-                *ptr = 0;
+                char *ptr2 = strrchr(cb_data->path, '/');
+                *ptr2 = 0;
         } else {
                 cb_data->path = malloc(strlen(path) + 2);
                 if (cb_data->path == NULL) {
@@ -3680,7 +3680,7 @@
                  void *private_data)
 {
 	char *new_path;
-	char *ptr;
+	const char *ptr;
 
         ptr = strrchr(path, '/');
         if (ptr) {
@@ -3690,8 +3690,8 @@
                                       "buffer for rmdir path");
                         return -1;
                 }
-                ptr = strrchr(new_path, '/');
-                *ptr = 0;
+                char *ptr2 = strrchr(new_path, '/');
+                *ptr2 = 0;
         } else {
                 new_path = malloc(strlen(path) + 2);
                 if (new_path == NULL) {
@@ -3782,7 +3782,7 @@
                  nfs_cb cb, void *private_data)
 {
 	char *new_path;
-	char *ptr;
+	const char *ptr;
 
         ptr = strrchr(path, '/');
         if (ptr) {
@@ -3792,8 +3792,8 @@
                                       "buffer for mkdir path");
                         return -1;
                 }
-                ptr = strrchr(new_path, '/');
-                *ptr = 0;
+                char *ptr2 = strrchr(new_path, '/');
+                *ptr2 = 0;
         } else {
                 new_path = malloc(strlen(path) + 2);
                 if (new_path == NULL) {
